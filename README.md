# Receipt-Reconciliation-system
Intelligent Receipt Reconciliation system using OCR, NLP, and LLMs to extract structured data from PDFs and receipt photos (items, totals, tax, payment). Matches receipts to stored bills via embeddings + key fields, fills missing values, and flags conflicts for review, enabling reliable cross checking across documents.
